"""multitenancy.routers — FastAPI endpoints.

These routers are defined but NOT auto-registered in src/main.py during
Phase 00.3 (per scope: main.py edits are owned by Phase 00.2.5 integration).
The router objects are importable for tests via dependency_overrides + a
local FastAPI app fixture.
"""

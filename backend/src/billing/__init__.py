"""Billing bounded context — Wave 0 SKELETON.

Wave 0 ships only `billing.credit_transactions` so the LLM gateway can write
the second leg of its atomic 3-currency cost ledger (per llm-gateway invariant
#7). Full billing context (credit_balances, pricing_table, tariff_plans,
subscriptions, invoices) lands in Wave 2-3 per contracts/billing/README.md.
"""

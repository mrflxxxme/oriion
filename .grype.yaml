# Grype ignore registry (audit-trail) — see .planning/decisions/ADR-014-security.md
# CI hook: .github/workflows/ci-security.yml job "sbom-and-grype" (anchore/scan-action,
# fail-build: true + severity-cutoff: high). Mirrors .trivyignore / pip-audit suppressions
# so the same unfixable starlette CVEs do not red-main across three scanners.
#
# starlette (installed 0.52.1) — UNREACHABLE: transitive via fastapi (pyproject pins
#   fastapi>=0.120,<0.130 → resolves starlette 0.x). The fixes land only in starlette
#   1.0.x / 1.1.0 / 1.3.0 / 1.3.1, which need a fastapi major bump. CVE drift (2026-06).
#   Removal trigger: the Wave-1 fastapi>=0.130 / starlette>=1.0 bump — bump both and
#   delete this block (mirrors .trivyignore justification).
ignore:
  - vulnerability: CVE-2026-48710
  - vulnerability: CVE-2026-48817
  - vulnerability: CVE-2026-48818
  - vulnerability: CVE-2026-54282
  - vulnerability: CVE-2026-54283

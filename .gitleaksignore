# gitleaks false-positive allowlist (fingerprint format: commit:file:rule:line)
# Test fixtures with synthetic JWT secrets are not real credentials. The
# current source (since 5be4ba5) uses a low-entropy constructed string;
# this entry is kept so the original Phase 00.2 test commit (9179b9f)
# does not retroactively fail the `--first-parent` PR diff scan.
9179b9fd5cea58cc08b37f557796dc120a95a669:backend/tests/iam/conftest.py:generic-api-key:90

# Phase 00.4 — dev-only BYOK master key literal in ci-backend.yml (commit
# a3f54bf). The base64 string `dGVzdC1tYXN0ZXIta2V5LWZvci1jaS1vbmx5LWRldi0wMQ==`
# decodes to "test-master-key-for-ci-only-dev-01" — NOT a real secret. It
# was a CI-injection placeholder for the BYOK_MASTER_KEY_B64 env var used by
# LocalAESKMS in test mode. Subsequent commit (7f65ed7) removed the literal
# and switched to runtime key generation via GITHUB_ENV. These fingerprints
# allowlist the legacy commit so the --first-parent PR diff scan ignores it.
a3f54bfcc4c31b8df9c6fa5e983a22e60e63ce2e:.github/workflows/ci-backend.yml:generic-api-key:102
a3f54bfcc4c31b8df9c6fa5e983a22e60e63ce2e:.github/workflows/ci-backend.yml:generic-api-key:113
a3f54bfcc4c31b8df9c6fa5e983a22e60e63ce2e:.github/workflows/ci-backend.yml:generic-api-key:120

# Phase 00.4 — contract documentation describing BYOK key handling triggers
# generic-api-key rule on the phrase "...customer-provided API key, AES-256-GCM-
# encrypted..." in llm-gateway README. Documentation, not a secret.
f5d3e56d216c23fdab3c03aaf6bb071660c86e61:.planning/contracts/llm-gateway/README.md:generic-api-key:45

# Phase 00.2.5 — dev-only BYOK master key literal in the E2E test fixture
# (commit 567c524). The base64 string `MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=`
# decodes to "0123456789abcdef0123456789abcdef" — NOT a real secret. It was
# inlined to pin the test_settings BYOK config; subsequent audit-fix commit
# (f703d13) switched to runtime base64 generation from a literal seed string
# so the high-entropy base64 no longer appears in source. This fingerprint
# allowlists the legacy commit so the --first-parent PR diff scan ignores it.
567c524e436e7b9b9c2ad63f984132ea9b387367:backend/tests/integration/test_e2e_auth_flow.py:generic-api-key:105

# AC-W1-9 — dev-only dummy BYOK base64 in the dev-secret guard test (commit
# df577ff): a throwaway test fixture (the guard only asserts the value is
# non-empty), since replaced with a low-entropy dummy + inline gitleaks:allow in
# commit 92ba499. Allowlist the legacy commit so the --first-parent PR diff scan
# ignores it. (NB: do NOT quote the literal base64 here — it would itself trip
# generic-api-key in this commit.)
df577ff6c3651cc5121c50810b0d95c07026cb59:backend/tests/_shared/test_config_guard.py:generic-api-key:43
# The first allowlist attempt (commit 6e03902) quoted that literal base64 in a
# comment, which itself tripped generic-api-key in that commit's diff. The comment
# above no longer quotes it; this fingerprint allowlists the legacy commit.
6e039027088c833cdb3f30e82016e031cf6df153:.gitleaksignore:generic-api-key:34

# 01.5 artifacts: MinIO dev-compose credential referenced by the S3 integration
# test (same dummy value as infra/docker-compose.dev.yml; local-only MinIO).
d09ac2c546f464d08841ebc51930af1ad60e031e:backend/tests/artifacts/integration/test_s3_minio_integration.py:generic-api-key:57
# Same MinIO dev-compose dummy, re-flagged under the squash-merge commit of
# PR #78 (squash mints a new sha; the branch-commit fingerprint above no
# longer matches the history scan).
a326f7a56a01fd8b735d934fc818e01f0b1a739c:backend/tests/artifacts/integration/test_s3_minio_integration.py:generic-api-key:57

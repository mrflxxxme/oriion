# Phase 00.7 — frontend audit dispositions

3-agent frontend-focused audit (designer + reviewer-frontend + a11y) per the
grill decision. Verdict: **approve** (no P0). Fixed-in-loop items below were
applied in commit C15; deferred items are lifted to Wave-1 (`01.1-retro.md`)
since Wave-0 ui-spec explicitly permits i18n placeholder mode + dark-first.

## Fixed in C15 (this PR)

| Item | Source | Fix |
|---|---|---|
| Feedback Badge text contrast fails AA (warning 1.6:1, success/info 3.0:1, danger 3.9:1 on -100 tints) | a11y A1 | Added `--color-{success,warning,info}-700` tokens; badges use `text-*-700` on `bg-*-100` (all ≥4.5:1). |
| Decorative blue `info` badges (role hints + running status) break single-accent | designer D1/D2 | Role hints → `default`; running status → `warning` (amber, brand-aligned). |
| Light-mode `text-tertiary` 2.8:1 / `text-secondary` 4.42:1 on surface | a11y A2/A3 | Light `--text-tertiary`→base-500, `--text-secondary`→base-600. |
| Destructive/CTA buttons used theme-flipping `text-page` (3.8:1 on rose; would fail amber in light) | a11y (live axe) | Mode-invariant `text-on-cta` (dark) + `text-on-danger` (white) tokens. |
| Mobile nav unreachable + inert hamburger < md (D4 §1) | reviewer #1 | AppShell self-manages mobile nav disclosure (hamburger `aria-expanded`/`aria-controls` → nav panel below md). |
| Markdown could emit competing `<h1>` + unhardened LLM links | a11y A6 / reviewer #6 | `components` override demotes md h1→h3/h2→h4/h3→h5 + forces `rel="noopener noreferrer nofollow"`. (react-markdown v10 already escapes raw HTML + sanitizes URL schemes — no XSS.) |
| Dead `prose-tokens` class | designer D4 | Removed; explicit `[&_*]` typographic overrides retained. |
| `forgot-password` dead `<a href="#" aria-disabled>` (focusable, jumps to top) | reviewer #8 | Non-interactive muted `<span>` placeholder. |
| TaskSubmit h1 `text-2xl` vs `text-3xl` elsewhere | designer D3 | Normalized to `text-3xl font-bold`. |
| AC7 only covered 4/5 routes by automated axe | a11y | Added axe assertions for cell-detail + task-submit in the @live demo → **all 5 routes axe-green** (live run passed). |

## Deferred to Wave-1 (`01.1-retro.md`)

- **i18n completeness** (reviewer #2–#5): result/progress status + artifact labels
  and a few existing-key call sites use inline Russian. Wave-0 ui-spec permits
  placeholder mode; complete the `t()` migration when the i18n library lands.
- Refresh-token response zod validation for boundary parity (reviewer #9).
- Textarea counter `aria-live` debounce to avoid per-keystroke SR announcements (reviewer #7 / a11y).
- Minor a11y polish: sort-button accessible name (A7), pagination `disabled`+`aria-disabled` redundancy (A8), single-item Tabs on cell detail (A9).
- Design nits: mono font for numeric/cost data (D5), interactive elevated-card hover (D6), sidebar hover-fill direction (D7), `gap-8` vs `gap-6` section rhythm (D8).
- Per-page designer loop (phase-spec Task 16) was compressed to one post-build
  visual review pass (timeline) — re-expand for higher-fidelity Wave-1 polish.

# Phase 01.8c — Autonomy hardening — PLAN

> Grown from [`01.8c-autonomy-hardening.md`](./01.8c-autonomy-hardening.md) seed-spec on the discuss-step
> (`/autonomy:run`, 2026-07-09, branch `claude/auto-01.8c-autonomy-hardening`). Implements ADR-040 D2/D3/D8/D9/D12.

## Goal

The ADR-037 autonomy contour gains **real evaluation independence** (roles as spawnable subagents),
**machine-precise contract stretch** (OpenAPI snapshot + CI diff), **doc-drift protection** (docs-freshness CI),
and the **Oriion code-rename** — so Wave 2 starts on a hardened dev-infra substrate. No product surface, no founder deps.

## Decomposition (2 PRs — see DECISIONS-LOG `01.8c` fork-1)

| PR | Scope items | Tripwire | Rationale |
|---|---|---|---|
| **PR-1 `01.8c`** (this plan, primary) | 1 native subagents · 2 OpenAPI-snapshot CI · 3 docs-freshness CI · 5 JOURNAL archival | `public_api_contracts` (item 2: new snapshot + non-normative api.yaml headers, additive) → ack-needed | Substantive infra; keep review focused. |
| **PR-2 `01.8c-rename`** (follow, same run if budget) | 4 Oriion rename (code + user-facing strings + role-prompts SemVer) | `auth_rbac_sessions` (iam TOTP/email strings) + `public_api_contracts` (role-prompts) → ack-needed | 63-file mechanical string-noise; decouple from infra review + iam-touch risk. |

## Scope (in) — PR-1

1. **Roles → native subagents** (D8): create spawnable `.claude/agents/<role>.md` for the 11 persistent roles (thin entry: frontmatter name/description/tools/model from `profile.md` + AGENTS.md routing + `tools-allowlist.md`; body = concise mandate + invariants + pointer to the `.claude/agents/<role>/` handbook, single source of truth). Update `judge-panel.md` + `escalation-policy.md` (drop "known gap: roles are handbooks not subagents"); refresh `.claude/AGENTS.md` spawn note.
2. **OpenAPI-snapshot** (D2): `scripts/autonomy/export_openapi.py` dumps `src.main:app.openapi()` → `.planning/contracts/openapi.snapshot.json` (sorted, stable). CI job `ci-contracts` regenerates + `git diff --exit-code`; drift = red. Mark handwritten `contracts/<ctx>/api.yaml` with a "design-time draft — non-normative (ADR-040 D2)" header; revise `tripwire.yaml` `public_api_contracts` comment to name the snapshot as the guarded artifact.
3. **Docs-freshness CI** (D9): `scripts/autonomy/check_docs_freshness.py` — a phase spec-file marked merged/complete in STATUS.md but still `Status: Pending` in its own file = red; README «текущая фаза» cross-checked vs STATUS.md. Wire into a CI job.
4. **JOURNAL archival** (D12): move pre-Wave-1 JOURNAL entries into `.planning/dev-log/archive/` keeping the rolling JOURNAL lean; leave a pointer.

## Out-of-scope

- **Item 4 Oriion rename** → PR-2 (`01.8c-rename`), same run.
- Any product surface; tripwire category changes (only comment/paths); MCP (→ 01.9, already merged).
- Renaming immutable historical records (ADR bodies, dated AUDIT-*, JOURNAL history) or the external memory-file-name reference — see DECISIONS-LOG fork-2.

## Acceptance criteria — PR-1

| AC | Критерий | Как проверяется |
|---|---|---|
| AC-01.8c-1 | 11 spawnable `.claude/agents/<role>.md` exist w/ valid frontmatter (name==dir, description, tools, model) | file presence + a lint that each parses + name matches dir; judge-panel/escalation "known gap" note removed |
| AC-01.8c-2 | `export_openapi.py` produces a stable snapshot; re-run is byte-identical | run twice, diff empty; snapshot committed |
| AC-01.8c-3 | `ci-contracts` CI job catches a synthetic breaking-diff (red on drift) | test: mutate a route, script exits non-zero |
| AC-01.8c-4 | handwritten `contracts/<ctx>/api.yaml` carry the non-normative header; tripwire comment revised | grep header present on each api.yaml; tripwire.yaml comment names snapshot |
| AC-01.8c-5 | `check_docs_freshness.py` catches a synthetic stale (merged phase w/ Status: Pending) | test: flip a merged phase spec to Pending → script exits non-zero; passes on real tree |
| AC-01.8c-6 | JOURNAL archived; rolling JOURNAL < prior size; pointer left | file moved under dev-log/archive/; JOURNAL head references archive |
| AC-01.8c-7 | Backend + frontend gates green; new scripts unit-tested | ruff/mypy --strict/pytest/bandit green; new tooling tests pass |

## Dependencies (verified by grep/git, not memory)

- ADR-040 (Accepted) · ADR-037 · ADR-023 (roles) · ADR-024 · ADR-010 (prompt SemVer, PR-2) — all present in `.planning/decisions/`. ✓
- Role handbooks `.claude/agents/<role>/{profile,system-prompt,tools-allowlist}.md` — present for 11 roles. ✓ (verified `ls`)
- `.planning/contracts/**` + `tripwire.yaml` — present. ✓
- FastAPI `src.main:app` exports `.openapi()` — standard. ✓

## Founder-зависимости

**НЕТ.** FOUNDER-RUNWAY: 01.8c «нет founder-зависимостей». RW-08 (funded keys) 🟢 for the ~$0.05 post-rename golden-smoke (PR-2). No parked deps.

## Tripwire-прогноз

- PR-1: `public_api_contracts` (item 2 touches `.planning/contracts/**` — new snapshot + additive non-normative headers). No migrations, no iam/rbac/billing/secrets. → ack-needed, single category, additive.
- PR-2: `auth_rbac_sessions` (iam email/TOTP user-facing strings) + `public_api_contracts` (role-prompts SemVer). → ack-needed.

## Evidence-гейты

- **adversarial audit (3 lenses)** — independent subagent spawns (SOUND / SECURE / NO-REGRESSIONS), refute-by-default → `evidence/adversarial_audit.json`.
- **live-golden:** N/A for PR-1 (no LLM path). PR-2: post-rename role-prompt golden-smoke ~$0.05 (prompts mention product name) — budget line below.
- No docker-integration needed (no DB/migration changes).

## Live-golden бюджет

- PR-1: **N/A ($0)** — deterministic dev-infra, no LLM path (ADR-040 D11).
- PR-2: **~$0.05** — one role-prompt golden-smoke after the rename to confirm prompt-name swap didn't break generation. Within dev_team caps.

## Мягкие AC (DEFERRED-VERIFICATION)

- PR-1: none deferred (all ACs hard-verified in-PR).
- If PR-2 (rename) does not complete this run, item-4 AC becomes PARTIAL → a DEFERRED-VERIFICATION row is added in the PR that ships the partial, or the rename lands as a follow-up chip. Not applicable to PR-1.

## Риски (top-5 pass)

- **R-12 (scope creep)** — the split caps PR-1 to substantive infra; rename isolated. ✓
- **R-05 (data leak)** / **R-04 (runaway cost)** — untouched (no data paths, no user LLM traffic; dev golden N/A). ✓
- **R-08 (regulatory)** / **R-11 (retention)** — not applicable to dev-infra. ✓
- New meta-risk: a bad subagent definition could degrade future autonomous runs — mitigated by thin-pointer design (handbook stays source of truth) + adversarial audit.

## ADR-refs

ADR-040 (D2/D3/D8/D9/D12) · ADR-037 (D2/D5 contour) · ADR-023 (11 roles) · ADR-024 (contracts) · ADR-010 (prompt SemVer, PR-2). All valid.

## DoR: PASS (11/11)

1 Goal ✓ · 2 Scope/out ✓ · 3 AC table machine/evidence-checkable ✓ · 4 Deps verified done ✓ · 5 Founder-deps = none ✓ · 6 Tripwire forecast ✓ · 7 Evidence gates named ✓ · 8 Live-golden budget (N/A PR-1 / $0.05 PR-2) ✓ · 9 Soft-AC registry (none in PR-1) ✓ · 10 Risks top-5 ✓ · 11 ADR-refs valid ✓.

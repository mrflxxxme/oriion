# Phase 01.8c — Autonomy hardening (seed-spec)

> Seed-spec per [DoR](../../DEFINITION-OF-READY.md) / [ADR-040](../../../decisions/ADR-040-execution-spec-contract.md). Дорастает до полной спеки на discuss-шаге.

- **Status:** ✅ **Complete** — PR-1 (autonomy infra D2/D8/D9/D12, merged #109 `857e09d`) + PR-2 (brand-rename **teamly→profiki** / «Профики», D3; OQ-09 решён 2026-07-10 = profiki). Scope: только потребительский бренд; `oriion` оставлен внутренним кодовым именем (founder-решение). Grown PLAN: [`01.8c-PLAN.md`](./01.8c-PLAN.md). · **Тип:** сервисная фаза (инфраструктура автономии, не продукт)

**Goal:** контур ADR-037 получает реальную независимость оценок, машинно-точную растяжку контрактов и защиту от дрейфа доков — реализация ADR-040 D2/D3/D8/D9.

**Scope (in):**
1. **Роли → нативные сабагенты** (D8): 11 профилей из `.claude/agents/<role>/profile.md` → спавнабельные `.claude/agents/<role>.md`; judge-panel и reviewer-линзы — реальные независимые спавны; обновить `judge-panel.md` + AGENTS.md (снять known-gap).
2. **OpenAPI-snapshot** (D2): скрипт экспорта живой FastAPI-схемы → `.planning/contracts/openapi.snapshot.json`; CI-job diff-проверки (breaking-diff = red + tripwire-сигнал); пометить рукописные `contracts/<ctx>/api.yaml` заголовком «design-time draft, не нормативен»; ревизовать комментарий в `tripwire.yaml`.
3. **Docs-freshness CI** (D9): лёгкая проверка — смерженная фаза со `Status: Pending` в своём spec-файле = red; README «текущая фаза» сверяется со STATUS.md.
4. **Oriion-ренейм в коде** (D3): main.py API title/docstrings, Makefile, тесты, `cd oriion` в README-инструкции; role-prompts — отдельными коммитами с SemVer-бампом (ADR-010) + обновление drift-check фикстур.
5. Maintenance: JOURNAL архивация в `dev-log/archive/` (D12).

**Out-of-scope:** любые продуктовые поверхности; изменение категорий tripwire (только комментарий/пути); MCP (01.9).

**ADR-refs:** ADR-040 (D2/D3/D8/D9/D12), ADR-037, ADR-023, ADR-010, ADR-024.

**Gating/Runway:** нет founder-зависимостей. Tripwire-прогноз: `secrets_keys_crypto` возможен (glob `**/.env*` не трогаем, но CI-workflows правим — вне категорий); role-prompts → drift-check.

**Evidence-гейты:** adversarial audit (3 линзы). Live-golden: **N/A** (нет LLM-пути) — но после ренейма role-prompts прогнать golden-smoke (промпты упоминают имя продукта) → бюджет ~$0.05.

**AC-направления:** судья-панель реально спавнит N независимых сабагентов (проверяемо по transcript); snapshot-CI ловит synthetic breaking-diff; docs-freshness CI ловит synthetic stale-статус; `git grep -i teamly` в АКТИВНОМ коде/промптах/UI/infra = 0 (carve-outs сохранены: `oriion` внутренний codename, `@teamly-ai` author-email-конвенция ADR-027, `teamly.to` внешняя дизайн-ссылка, memory-file-name, immutable ADR/AUDIT/JOURNAL-история, filesystem-путь `TEAMLY_RU`).

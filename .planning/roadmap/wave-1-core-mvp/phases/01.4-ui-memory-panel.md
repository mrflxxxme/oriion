# Phase 01.4-ui — Memory panel «Что помнит [агент]» (seed-spec)

> Seed-spec per [DoR](../../DEFINITION-OF-READY.md) / [ADR-040](../../../decisions/ADR-040-execution-spec-contract.md). Дорастает до полной спеки на discuss-шаге.

- **Status:** ✅ Code-complete + locally verified (2026-07-09, `/autonomy:run`, branch `claude/auto-01.4-ui-memory-panel`) · **Queue:** выполнена первой (reorder product-first, см. DECISIONS-LOG run-2026-07-09) · server-verify после deploy

**Goal:** пользователь видит и контролирует память команды: панель «Что помнит [агент]» с просмотром/редактированием/удалением записей cell- и role-memory (закрывает grill Q6 фазы 01.4).

**Scope (in):** frontend-панель поверх готового API `/api/v1/memory/*` (list/search/add/delete уже live с 01.4); отображение source (`manual`/`filter_agent`/`conversation_summary`); удаление с подтверждением; edit = delete+add (append-only семантика бэка — уточнить на discuss); доступ per RBAC 01.7 (Member видит, права на delete — форк discuss); tokens v0.2 + axe AA per `ui/REVIEW-CHECKLIST.md`.

**Out-of-scope:** новые backend-эндпоинты (если discuss не докажет необходимость PATCH); PARA-workspace (Wave 3); настройки auto-extraction.

**ADR-refs:** ADR-011, ADR-016, ADR-031.

**Gating/Runway:** нет. Tripwire-прогноз: чистый frontend → вне растяжки (auto-merge при зелёных гейтах).

**Evidence-гейты:** frontend unit + e2e (axe). Live-golden: **N/A** (UI-фаза, LLM-путь не трогается). Бюджет $0.

**AC-направления:** панель отображает реальные записи обоих уровней; delete реально удаляет (проверка через API); RBAC-матрица прав соблюдена; axe 0 serious/critical; UI-SPEC сгенерирован через `gsd:ui-phase` и соблюдён.

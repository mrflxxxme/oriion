# Phase 01.9a — DLP activation (INN precision-tune + flip flags ON) — PLAN

> Split from 01.9 seed (`01.9-mcp-servers.md`) per DECISIONS-LOG `01.9` arch entry. Autonomous run 2026-07-09.
> **DoR: PASS (11/11)** — checklist at bottom. Precondition for 01.9b connectors (ADR-040 D10).

## 1. Goal (verifiable)
RU-PDn DLP становится точным и **включённым по умолчанию**: INN-детектор даёт **FP ≤1%** на golden-корпусе PII, оба флага (`security_dlp_enabled` + `security_injection_scan_enabled`) → **default True**, и существующий агент-пайплайн продолжает работать с включённой защитой. Закрывает **DV-04 + DV-05**.

## 2. Scope
**In:** (1) golden-корпус PII (`backend/tests/security/corpus/` — labeled positives + hard-negatives: checksum-passing 10-значные не-ИНН — invoice/order/phone-подобные); (2) precision-tune INN-детектора (`backend/src/security/detectors/pii.py`) — **context-aware**: checksum-pass **И** контекст (keyword «ИНН/инн» в окне ИЛИ структурный `ИНН:`-паттерн) для флага; bare checksum-число без контекста = не identifiable PII → не флагуем; сохранить recall на labeled positives; (3) precision-тест `assert FP ≤ 0.01`; (4) flip обоих флагов default→True (`_shared/config.py`); (5) обновить/починить тесты, зависящие от flags-OFF, + integration-проверка, что DLP-screen на реалистичном output не ломает task-success path.
**Out (адресовано):** MCP-коннекторы + capability-gate activation + creds-store → **01.9b**. SNILS/passport/phone/email tuning (checksum уже строгий у SNILS; остальные format-only — recall-first, вне scope FP-цели). ML layer-A → future (port seam).

## 3. Acceptance criteria
| AC | Критерий | Как проверяется |
|---|---|---|
| AC-01.9a.1 | Golden-корпус существует: ≥N positives (с/без контекста) + ≥M hard-negatives (checksum-passing не-ИНН) | файл(ы) `backend/tests/security/corpus/*` + тест их грузит |
| AC-01.9a.2 | INN-детектор FP ≤1% на hard-negatives корпуса | precision-тест `assert fp_rate <= 0.01` (green) |
| AC-01.9a.3 | Recall на labeled INN-positives не деградирует ниже baseline (контекстные ИНН ловятся) | recall-тест на positives |
| AC-01.9a.4 | Оба флага default True | grep `_shared/config.py` + тест дефолтов Settings |
| AC-01.9a.5 | Агент-пайплайн работает с флагами ON: DLP-screen не блокирует легитимный output; DLP блокирует synthetic-ПДн | integration/unit: реалистичный output через `_dlp_screen_text`+`DlpGuard`; synthetic-INN-with-context → DlpViolation |
| AC-01.9a.6 | injection-sanitize ON не ломает web_search/read_url output (B1 strip только на synthetic-injection) | unit на `_sanitize_external` с флагом ON |
| AC-01.9a.7 | DV-04 + DV-05 переведены в «Закрытые» с evidence-ссылкой | `DEFERRED-VERIFICATION.md` обновлён |

## 4. Dependencies (verified by fact)
- 01.6 security substrate — MERGED (#84): `backend/src/security/detectors/pii.py` INN-10/12 checksum, `services/dlp.py` A3, `capability.py`, flags in `_shared/config.py:377-398` default False. ✓ (discovery-verified file:line)
- Runtime seams (`runtime/security_guardrails.py`, `orchestrator.py` output_dlp, `queue/actor.py:127-136` flag gate, `web_search_runner.py` injection) — MERGED. ✓

## 5. Founder-deps (RUNWAY)
Нет. (RW-08 LLM keys 🟢 нужны только для опционального server-verify; корпус+тюнинг детерминированы.)

## 6. Tripwire-прогноз
Затрагивает `backend/src/security/**` (не в растяжке) + `backend/src/_shared/config.py` (не в растяжке) + `backend/tests/**` (не в растяжке). **Прогноз: tripwire exit 0 → auto-merge.** (Проверю classify_tripwire; если config-flag-flip как-то матчит secrets_keys_crypto — ack.)

## 7. Evidence-гейты
Adversarial audit — **1 SECURE lens** (refute-by-default: «сырое ПДн утекает мимо tuned-детектора?» + «tuning создал false-negative-дыру?») — независимый спавн. Docker-integration: DLP-screen на success-path (real PG не нужен для DLP-логики; unit+in-proc integration достаточно). Judge-panel: N/A (tuning — узкий fork).

## 8. Live-golden бюджет
**Gate: N/A ($0)** — FP/recall детерминированы на корпусе; DLP-логика — unit/integration. Live-валидация флагов-ON = **consolidated server-verify** на VPS (реальный demo-brief с флагами ON, ~$0.3, funded VPS keys) на post-01.9 checkpoint — не gate.

## 9. Мягкие AC (DEFERRED-VERIFICATION)
Нет новых мягких AC. Наоборот — **закрывает** DV-04/DV-05 (evidence = FP≤1% precision-тест green + флаги ON в дефолте).

## 10. Риски (top-5)
- **R-05 (data leak) — прямое смягчение**: DLP ON = активная защита от утечки ПДн (было substrate-only). Тюнинг НЕ должен создать false-negative-дыру (audit SECURE-lens проверяет recall). R-04/08/11/12 не затронуты.

## 11. ADR-refs
ADR-039 (security guardrails), ADR-014 (§DLP), ADR-040 (D10 — precision→ON→then connectors). Все валидны. Новых ADR нет (tuning + flag, не архитектура).

### DoR checklist
1 Goal✓ · 2 Scope/out✓ · 3 AC-table✓ · 4 Deps(fact/discovery)✓ · 5 Founder-deps(none)✓ · 6 Tripwire(exit0 expected)✓ · 7 Evidence(SECURE audit)✓ · 8 Live-golden(N/A gate; server-verify separate)✓ · 9 Soft-AC(closes DV-04/05)✓ · 10 Risks(R-05 mitigated)✓ · 11 ADR-refs✓ → **PASS (11/11)**

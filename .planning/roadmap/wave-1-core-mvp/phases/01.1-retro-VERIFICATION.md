---
phase: 01.1-retro
verified: 2026-06-18T00:00:00Z
status: passed
score: 9/9 scoped must-haves verified (7 closed + AC-W1-4/5/9 this-session fixes); deferred set honestly marked
re_verification: null
scope: SCOPED-COMPLETE (founder-locked) — non-deferred AC-W1 pins delivered in code + CI green + docs synced. Deferred items are named follow-ups, NOT part of the bar.
gaps: []
human_verification:
  - test: "Live golden AC8/AC9/AC10 on full stack (docker dev + worker + funded DeepSeek key)"
    expected: "brief ≥1500w (AC9), $-cap held (AC10), dispatch p95 ≤1s (AC8 reframed); generation wall-clock = tracked SLI"
    why_human: "Requires funded provider keys + running stack; explicitly a founder-action, NOT blocking the scoped flip"
  - test: "AC-W1-9 staging cutover — no secret on VM disk + Lockbox version-bump pickup"
    expected: "backend+worker boot reading secrets directly from Lockbox; rotation via version-bump + SIGHUP picks up new DSN with no redeploy"
    why_human: "Deploy/runtime validation on staging VM (chip task_8d5ce94c); code path is closed + unit-tested"
  - test: "backend/Dockerfile:41 real RU_TRUSTED_CA_SHA256 before prod image build"
    expected: "real CA pin substituted for placeholder"
    why_human: "Build-time founder action; not a code gap"
---

# Phase 01.1-retro: Wave-0 Hardening Backlog (AC-W1 Pin Block) — Verification Report

> **🟢 Re-verification addendum (2026-06-19, session `ecstatic-boyd-7ae652`):** the deferred set named below is no longer residual. The obs/IaC pins **AC-W1-2/13/14/15 are now CLOSED + live-proven** in [PR #66](https://github.com/mrflxxxme/oriion/pull/66) (per-delegation `task_steps` + real `record_llm_cost` cost; Loki S3+90d+compactor + `audit_log` archival job; Alertmanager Telegram+PagerDuty receivers). Live proof harness `infra/observability/proof/` ALL PASS (chunks→bucket, archival upload round-trip, critical-alert routing). **Only AC-W1-3 remains deferred → Wave-2** (founder decision, ADR-029). The phase is **COMPLETE** modulo PR merges (#65 + #66) + the deploy-time secret swap (same boundary as AC-W1-9). The Deferred Set table below is the 2026-06-18 snapshot — kept for audit trail.

**Phase Goal:** The Wave-0 audit-deferred AC-W1 hardening pins are closed on a hardened horizontal base, with deliberately-deferred items explicitly tracked (NOT silently dropped).
**DoD bar:** SCOPED-COMPLETE (founder-locked) — non-deferred pins delivered in code + CI green + docs synced; deferred items are named follow-ups.
**Verified:** 2026-06-18 (session `interesting-knuth-f649a6`)
**Status:** passed — **GOAL ACHIEVED (scoped)**
**Re-verification:** No — initial goal-backward verification.

## Verdict

**GOAL ACHIEVED** for the SCOPED bar. Every AC claimed CLOSED is genuinely implemented in code (verified file:line, not by trusting prior docs). The two this-session fixes that the post-merge audit surfaced as P1/P2 (AC-W1-4 relay dead-code; AC-W1-5 missing real-PG test) are now real. AC-W1-9's SIGHUP partial-apply P1 is fixed in code. The deferred set (AC-W1-2/13/14/15 → obs-IaC; AC-W1-3 → Wave-2) is honestly marked as residual in code comments, the spec closeout note, and STATUS.md — no phantom-closed claims, no silent drops. Docs (STATUS/JOURNAL/HANDOFF/ADR-036) are synced.

## Goal Achievement — Observable Truths

| # | Truth (scoped) | Status | Evidence (file:line) |
|---|----------------|--------|----------------------|
| 1 | AC-W1-7: single `CoordinatorDeps`, `CoordinatorDepsLike` shim collapsed; NullTeamProvisioning no-op default | ✓ VERIFIED | `agents/tools/delegate.py:98` single dataclass; `agents/coordinator.py:27,103,117` imports+uses+re-exports the one type; `orchestrator.py:247-251` uses it; `agents/services/team_provisioning_service.py:135` Null impl + `:158` `NULL_TEAM_PROVISIONING` singleton; `iam/services/auth_service.py:109,199` default wiring |
| 2 | AC-W1-8: `AgentSlug` enum constrains delegation slug at validation | ✓ VERIFIED | `agents/tools/delegate.py:39` `class AgentSlug(StrEnum)` (4 members); `:66` `DelegateInput.target_agent_slug: AgentSlug` → bogus slug raises Pydantic ValidationError, not at dispatch |
| 3 | AC-W1-17: `src/agents` ≥85% per-module gate wired in CI + real tests | ✓ VERIFIED | `.github/workflows/ci-backend.yml:204-208` `pytest tests/agents --cov=src/agents --cov-fail-under=85` in the per-module loop; real tests present (`test_archetype_service.py`, `test_leaf_factories.py`, `test_delegate_tool.py`, `test_team_provisioning_service.py`, `test_coordinator_agent.py`, …) |
| 4 | AC-W1-18: Yandex XML parser XXE-hardened + real parse (not `[]` stub) | ✓ VERIFIED | `mcp/tools/web_search.py:288-291` `resolve_entities=False/no_network=True/load_dtd=False`; `:314-323` real flatten into `WebSearchResult`; `read_url` present in `mcp/tools/read_url.py` |
| 5 | AC-W1-10: GigaChat OAuth refresh-after-expiry test (real `_ensure_token`) | ✓ VERIFIED | `tests/llm_gateway/test_provider_gigachat_refresh.py:32-66` forces expired token (`:57-58`), asserts `oauth_calls==1` (`:65`) + chat carries `Bearer fresh-token` (`:66`); plus concurrent-lock test (`:69-93`); drives the REAL provider path |
| 6 | AC-W1-4 (this session): outbox relay is no longer dead code — scheduled, self-rescheduling, per-row poison-isolation, deterministic order | ✓ VERIFIED | `runtime/queue/outbox_relay.py:154` real `@dramatiq.actor relay_outbox_actor`; self-reschedules in `:181-182 finally`; `worker.py:73` imports it + `:80` `relay_outbox_actor.send()` boot-kick; per-row try/except+continue (`:110-120`) + single end-commit (`:123`); `(created_at, id)` order (`:101`) |
| 7 | AC-W1-5 (this session): real-PG testcontainers cancel-cascade + SSE-order integration test | ✓ VERIFIED | `tests/tasks/test_cancel_cascade_integration.py:97` `[integration, commit_required]`; PART A asserts cascade rows status=cancelled/completed_at set (`:240-249`) + single `oriion.tasks.cancelled.v1` outbox row (`:264-273`); PART B asserts SSE `[task.started…task.completed]` (`:402-410`) |
| 8 | AC-W1-9 closed-in-code: Lockbox direct-read + boot guard; this session's fix drops DB/Redis caches on refresh | ✓ VERIFIED | `_shared/lockbox.py:4-103` payload REST API + IAM-metadata token, wired as settings source; `config.py::_guard_no_dev_secrets` boot guard; `_shared/secret_refresh.py:85-110` `_reset_db_redis_caches` (disposes old engine/redis, clears lru_caches), called `:123` before `apply_secret_state`; test `tests/_shared/test_secret_refresh.py` |
| 9 | Deferred set honestly NOT claimed closed (named follow-ups) | ✓ VERIFIED | AC-W1-2 `orchestrator.py:88` "Wave-1+ task_steps concern, see AC-W1-2"; AC-W1-13 `dispatch.py:218` cost still `estimate_credits` (not record_llm_cost); spec closeout note + STATUS.md:19 name AC-W1-2/13/14/15 residual + AC-W1-3→Wave-2 |

**Score:** 9/9 scoped truths verified.

## Deferred Set — Honestly Marked (verified residual, NOT closed)

| AC | Claimed state | Code reality | Honest? |
|----|--------------|--------------|---------|
| AC-W1-2 | deferred (no per-delegation TaskStep write) | `orchestrator.py:88` explicit Wave-1+ comment; no `task_steps` write found | ✓ |
| AC-W1-13 | deferred (cost still estimate, worker metric exposition pending) | `dispatch.py:218` uses `estimate_credits`; `:87` notes record_llm_cost not yet the cost source | ✓ |
| AC-W1-14 | deferred (loki retention/archival not done) | bucket provisioned per spec; no retention/archival job in code | ✓ (IaC) |
| AC-W1-15 | deferred (alertmanager stub) | stub receiver per spec | ✓ (IaC) |
| AC-W1-3 | deferred → Wave-2 (ADR-029 Proposed) | no Master-Agent schema; correctly punted | ✓ |

None of the deferred items are claimed closed anywhere in code, spec, or STATUS. The prior "closed in #48/49/50" framing for the obs/IaC pins was explicitly called out as a phantom in the spec closeout — corrected, not propagated.

## Docs Synced

| Artifact | Status | Evidence |
|----------|--------|----------|
| STATUS.md | ✓ | `:11,:17-19` 01.1-retro VERIFIED-COMPLETE (scoped), #58-61 merged, base #53 (`6b40084`), closed set + deferred residuals + founder-actions all named |
| JOURNAL.md | ✓ | `:3` 2026-06-18 entry "Phase 01.1-retro closeout — post-merge audit + fix-to-green + verified-complete"; refs ADR-036 + audit `6b40084..HEAD` |
| HANDOFF.md | ✓ | present (rewritten); referenced from STATUS |
| ADR-036 | ✓ | `decisions/ADR-036-outbox-relay-self-scheduling.md` present + indexed in `decisions/README.md:38` (AC-W1-4 relay-gap closeout, Accepted) |

## Anti-Patterns Found

| File | Pattern | Severity | Disposition |
|------|---------|----------|-------------|
| `outbox_relay.py:110-120` | per-row `try/except…continue` | ℹ️ Info | Intentional poison-row isolation (the AC-W1-4 hardening), not a swallow — failed row stays unpublished + retried |
| (none) | TODO/FIXME/NotImplementedError/`return []` stub | — | No blocker anti-patterns in this-session files |

P2/P3 backlog (10 items: cancel_task terminal-status guard, read_url DNS-rebinding SSRF, web_search body-size cap, outbox RLS/grant, BFS cycle guard, Yandex `.//error` XPath, `recover=True` truncated-XML) are named in the spec + PR body as follow-up chips — out of the scoped bar, NOT regressions, NOT goal-blocking.

## CI Evidence (spot-checked, not re-run)

Test files backing the claimed green suite all exist with substantive assertions:
ruff/mypy-strict + unit (683 pass) + integration (25 pass, real testcontainers PG) per session note. The integration test for AC-W1-5 is marked `[integration, commit_required]`, consistent with the 25-integration count. Verified by substance, not re-execution (per instructions).

## Human Verification Required (founder-actions — NOT blocking the scoped flip)

1. **Live golden AC8/AC9/AC10** on funded DeepSeek + full stack — generation-quality validation.
2. **AC-W1-9 staging cutover** — no-secret-on-disk boot + Lockbox version-bump pickup (chip `task_8d5ce94c`).
3. **Dockerfile:41 real RU_TRUSTED_CA_SHA256** before prod image.

All three are explicitly named in the spec + STATUS as founder deploy/build actions, distinct from the code-complete scoped bar.

## Gaps Summary

No gaps blocking the SCOPED bar. Every CLOSED AC is implemented in code (not merely claimed), the two this-session relay/test fixes are real and wired, AC-W1-9's cache-reset fix is present, and the deferred set is honestly tracked as residual rather than silently dropped. The only outstanding items are deploy/build/live-validation founder-actions that the founder-locked DoD explicitly excludes from the flip.

---

_Verified: 2026-06-18 · Verifier: Claude (gsd-verifier) · goal-backward, skeptical, READ-ONLY on src/_

# PLAN — Phase 01.5 Артефакты (autonomous, ADR-037)

> Runner: session `charming-kepler-c814fe`, branch `claude/auto-01.5-artifacts`.
> Resolved decisions: см. [01.5-artifacts.md §Discuss](./01.5-artifacts.md) + [ADR-038](../../../decisions/ADR-038-artifacts-envelope-schema.md) + DECISIONS-LOG.
> `wide_fork: F2 → resolved (judge-panel, ADR-038)` · `escalated: RQ-20260701-001, RQ-20260701-002 (non-blocking, leans executing)`

## Task decomposition (atomic commits, ADR-027 format, Pipeline-role field)

| # | Task | Role | Deliverable |
|---|---|---|---|
| C1 | Migration `artifacts_0001` — 7 tables per ADR-038 (FORCE RLS, immutability grants, composite FKs, storage_kind evolution comment) | backend-implementer | `backend/migrations/versions/artifacts/0001_artifacts_core.py` |
| C2 | Models + repositories (`src/artifacts/models.py`, `repositories/`) — mypy --strict, mirror `src/memory` layout | backend-implementer | SQLAlchemy models, typed repos |
| C3 | Yjs service — pycrdt dep + merge-under-FOR-UPDATE write path, snapshot commit, compaction; unit tests | backend-implementer | `services/yjs.py` |
| C4 | S3 port + boto3 adapter (presign POST/GET, HEAD-verify, key builder) + fake for unit; s3_objects lifecycle service | backend-implementer | `services/s3.py`, `ports.py` |
| C5 | Envelope service (create/version/soft-delete, usage counter, events) + resolver `artifact://` | backend-implementer | `services/artifacts.py`, `services/resolver.py` |
| C6 | Routers `/api/v1/artifacts/*` + schemas + app wiring; unit tests | backend-implementer | `routers/`, `schemas.py` |
| C7 | Integration tests (real PG + MinIO): RLS isolation, race-retry, S3 flow, resolver, task_artifacts refs, usage consistency | tester (backend-implementer executes) | `tests/artifacts/integration/` |
| C8 | Contracts rewrite (schema.sql / api.yaml / events.yaml / README) to match implementation | backend-implementer | `.planning/contracts/artifacts/*` |
| R1 | Reviews: reviewer-backend + reviewer-security (parallel) → fixes | reviewers | review fixes |
| G1 | Gates: make lint typecheck test + integration; evidence emission (judge_panel, docker_integration, adversarial_audit) + manifest | verifier | `evidence/*.json` |
| A1 | Adversarial audit — 3 lenses (SOUND/SECURE/NO-REGRESSIONS), refute-by-default | reviewer roles | audit verdicts → fixes |
| E1 | Exit ritual: JOURNAL append + HANDOFF rewrite + STATUS/PHASES update | runner | `.planning/*` |

## Sequencing

C1→C2→{C3,C4 parallel}→C5→C6→C7→C8 → R1 → fixes → G1/A1 → E1 → PR → tripwire classify (expected exit 10: `db_migrations` + `public_api_contracts`) → RUN-QUEUE ack-needed + notify → **NO auto-merge** (жду founder /ack).

## Risks / guards

- pycrdt — новая зависимость: pin версии, проверить wheels для Windows+Linux CI.
- MinIO в CI GitHub: ci-backend job может не иметь MinIO → integration S3-тесты маркируются `integration` (локальный Docker-гейт, evidence docker_integration.json), unit используют fake port.
- Stagnation kill-switch 30 мин (ADR-015 §5) действует на каждую задачу.
- Budget: judge-panel уже потрачен (~4 subagent runs); implementation в основной сессии + review subagents — в пределах dev_team per-day soft cap $30.

# PLAN — Phase 01.7 RBAC (Owner + Member) (autonomous, ADR-037)

> Runner: branch `claude/auto-01.7-rbac` (off `origin/main` f7f5698).
> Resolved decisions: см. [01.7-rbac.md §Discuss](./01.7-rbac.md) + ADR-014 §1 + DECISIONS-LOG (грил 2026-07-03 founder-approved Option A; impl forks F1/F2 logged 2026-07-04).
> `wide_fork: нет (design pre-resolved ADR-014 §1)` · `escalated: нет` · `tripwire target: founder ack` (src/rbac + auth surface + migration ALTER → auth_rbac_sessions + db_migrations, not a pure-CREATE).

## Task decomposition (atomic commits, ADR-027 format, Pipeline-role trailer)

| # | Task | Role | Deliverable |
|---|---|---|---|
| C1 | Migration + contract + ORM: `artifacts/0002_artifact_visibility_stub.py` (ADD COLUMN `visibility` NOT NULL DEFAULT `'cell-shared'` + CHECK) · `contracts/artifacts/schema.sql` 1:1 · `artifacts/models.py` column + CHECK | coder | migration + contract + model |
| C2 | Enforcement layer: `rbac/services/authorization_service.py` (+`has_cell_permission`) · `rbac/exceptions.py` (`RbacError`/`PermissionDenied`) · `rbac/deps.py` (`require_cell_permission` factory) · `main.py` RbacError handler | coder | rbac guard + exceptions |
| C3 | Apply guards: `cells.py` (invite/role-change/remove Owner-guarded + new `DELETE /cells/{cell_id}` archive) · `billing.py` (`billing.view` on cell-scoped reads) | coder | router enforcement |
| C4 | Tests: `tests/rbac/test_cell_permission.py` + `tests/rbac/test_require_cell_permission.py` (unit) · `tests/integration/test_rbac_enforcement.py` (real PG) · update `tests/multitenancy/test_cells_router.py` guard deps | tester | tests |
| C5 | Phase spec + PLAN + DECISIONS-LOG (impl forks F1/F2) | runner | `.planning/*` |
| G1 | Gates: ruff + mypy --strict + bandit + unit + integration + `src/rbac` cov; adversarial audit 3 lenses → `evidence/adversarial_audit.json` + manifest (head_sha=final) | verifier | `evidence/*.json` |
| E1 | Exit ritual: JOURNAL append + HANDOFF rewrite | runner | `.planning/JOURNAL.md`, `.planning/HANDOFF.md` |

> Commits grouped in logical units per the task: (C1) migration+contract+model, (C2+C3) enforcement, (C4) tests, (C5) phase-docs.

## Sequencing

Gap-analysis (read rbac/multitenancy/artifacts/iam) → C1 → C2 → C3 → C4 → gates (all green) → C5 docs → G1 evidence → E1 exit ritual → PR (do NOT auto-open — orchestrator opens) → tripwire classify (**founder ack**: migration ALTER + auth surface) → founder ack → squash-merge → post-merge health.

## Risks / guards

- **Two role stores confusion** — `role_assignments` (empty) vs `cell_members` (populated). Guard reads `cell_members` (F1). Verified by integration owner-grant / editor-deny / non-member-deny.
- **Cross-tenant grant leak** — mitigated: RLS-scoped session + explicit `cell_id`/`user_id` filter; a caller can only match their own cell's row. SECURE-lens central invariant.
- **Existing router tests breaking on new guard deps** — `cells_router` + `billing_e2e` updated: cells_router overrides guard deps to grant (owner path); billing_e2e registers a real owner (bootstrap → owner has billing.view) so it exercises the guard for real. Full suite re-run after C3/C4.
- **Migration ALTER = founder ack** — expected + correct; not avoided. Column is additive with a default (no backfill, existing rows safe).
- **Reads must stay open** — flat visibility: `get_cell`/`list_members` + global billing (`credit-rate`/`plans`) NOT guarded. Asserted open for both roles.
- **Docker founder-controlled** — up for this run; integration + billing-E2E ran green. Unit-only path needs no Docker.

## Follow-ups (P2/P3, deferred)

- **P3 — `has_permission` (role_assignments) is now dead in Wave-1.** Kept intentionally as the seam for future workspace-scoped/delegated grants (Wave 2). If Wave 2 never uses it, revisit for removal.
- **P3 — cells router uses path `cell_id` while the guard resolves the caller's single Wave-0 cell** via tenant context; they coincide in Wave-0 (1 user → 1 cell). When Wave-1+ adds multi-cell membership + an `active_cell` JWT claim, the guard's `get_current_cell_id` must reconcile with the path param (documented in `tenant_context.py`). No action now (Wave-0 invariant holds).
- **P3 — billing writes** don't exist yet in Wave-1; when they land, gate with `billing.manage` (Owner/billing-role only), same factory.

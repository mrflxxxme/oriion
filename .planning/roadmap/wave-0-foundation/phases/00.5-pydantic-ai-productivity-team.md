# Phase 00.5 — Pydantic-AI runtime + horizontal `productivity-core` team-preset

> **Revision 2026-05-15:** anchor changed: WB-Селлер vertical → horizontal `productivity-core` team. WB-Селлер team-preset переезжает в Wave 2 per [ADR-017](../../../decisions/ADR-017-vertical-templates.md) revision. See [Session-decision](../../../JOURNAL.md).

- **Owner:** Senior Backend · **Duration:** 5 дней · **Status:** Pending
- **Level:** B (per P-INIT-1) — cross-link к contracts + inline agent definitions skeleton + delegation tool signature + SSE event-types + task-step state machine + deep role-prompts in `contracts/role-prompts/`
- **Contracts (P-INIT-2 cross-link):**
  - [agents](../../../contracts/agents/) — `schema.sql` (agent_archetypes, team_presets, agent_instances) + `api.yaml` (team creation, archetype listing) + `events.yaml` + `README.md`
  - [tasks](../../../contracts/tasks/) — `schema.sql` (tasks, task_steps, task_artifacts) + `api.yaml` (task lifecycle endpoints) + `events.yaml` (task.created/started/step_completed/completed) + `README.md` (task state machine invariants)
  - [memory](../../../contracts/memory/) — **SKELETON** status; inline minimum reference (cell.memory_entries DDL уже в Phase 00.3 per-cell schema template; полная memory contract Wave 1)
  - [**role-prompts**](../../../contracts/role-prompts/) — NEW contract type: deep role-prompt files (9-section structure) для 4 ролей `productivity-core` (Coordinator + Researcher + Writer + Analyst). First-draft в этой фазе; hardening pass в Phase 01.1 retro.
- **Tools (per P-AUDIT-3):** `agents.team_create`, `agents.instance_create`, `agents.instance_list`, `tasks.create`, `tasks.get`, `tasks.cancel`, `tasks.step_respond`, `llm.chat_completions`, `memory.cells_search`, `memory.cells_upsert`, built-in `web_search`, `read_url`, Bash/Read/Write/Edit

## Goal

Минимальный агентный runtime через Pydantic-AI. Первый team-preset — **horizontal `productivity-core` («Твои личные ассистенты»)** с 4 ролями: Coordinator + Researcher + Writer + Analyst. Пользователь ставит задачу Coordinator'у → он декомпозирует на 3 sub-task → делегирует Researcher/Writer/Analyst → возвращает ответ + 3 артефакта (brief.md + competitive-matrix.md + content-plan.md) через SSE stream.

**Demo-сценарий (фиксированный):** «Market & content brief для нового продукта». User input: «Запускаем платформу AI-команд для SMB в РФ. Сделай нам market brief + контент-план первого месяца».

**Acceptance metric:**
- End-to-end demo latency p95 ≤ **120 sec** (3 parallel sub-task delegations)
- SSE event delivery latency < 200ms p95
- Cost rollup accuracy 100% (parent.cost == sum(children.cost))
- Demo cost ≤ 30¢ per run (~10K input + 8K output tokens распределённо)
- Артефакты соответствуют contract: `brief.md` ≥1500w, `competitive-matrix.md` ≥5×4 markdown-таблица, `content-plan.md` 10 постов с outline

## Dependencies

- 00.3 (DB + cell schemas + audit) — Done
- 00.4 (LLM gateway + MCP framework + built-in tools) — Done

## Contracts cross-reference (P-INIT-2)

| Контракт | Files | Что berём |
|---|---|---|
| **agents** | `schema.sql` | `agents.agent_archetypes` (formal01, creative01, hoodie01 + future), `agents.team_presets`, `agents.agent_instances` (cell-scoped) |
| **agents** | `api.yaml` | `POST /api/cells/{id}/teams`, `GET /api/cells/{id}/agents`, `GET /api/agent-archetypes` |
| **agents** | `events.yaml` | `tech.oriion.agents.team_provisioned.v1`, `tech.oriion.agents.instance_created.v1` |
| **tasks** | `schema.sql` | `tasks.tasks` (id, cell_id, agent_id, user_id, prompt, status, result, parent_task_id, cost_credits), `tasks.task_steps` (step_type, payload, tokens, cost), `tasks.task_artifacts` |
| **tasks** | `api.yaml` | `POST /api/cells/{id}/tasks`, `GET /api/cells/{id}/tasks/{tid}`, `GET .../stream` (SSE), `POST .../cancel` |
| **tasks** | `events.yaml` | `tech.oriion.tasks.created.v1`, `tasks.started`, `tasks.step_completed`, `tasks.completed`, `tasks.cancelled` |
| **tasks** | `README.md` | Task state machine (pending → running → completed/failed/cancelled), max delegation depth = 5, cost rollup contract |
| **memory** | `README.md` | **SKELETON** — Wave 1 contract. Wave 0 uses Phase 00.3 per-cell `cell_<uuid>.memory_entries` table directly (pgvector + HNSW already provisioned) |

## Phase-specific inline (НЕ покрыто DRAFT-READY contracts)

### Pydantic-AI Agent definitions skeleton

```python
# backend/src/agents/coordinator.py
from pydantic_ai import Agent, RunContext
from src.agents.tools.delegate import delegate_task, DelegateInput, DelegateResult
from src.agents.tools.builtin import web_search, read_url
from src.llm_gateway.pydantic_ai_model import LLMGatewayModel

class CoordinatorDeps(BaseModel):
    """Dependency injection container per Pydantic-AI pattern."""
    cell_id: UUID
    task_id: UUID
    user_id: UUID
    available_agents: list[AgentInstance]   # team members
    max_delegation_depth: int = 5

class CoordinatorOutput(BaseModel):
    """Structured output schema — enforces type-safe agent responses."""
    summary: str                            # 1-2 sentence answer
    delegation_plan: list[str]              # which agents engaged
    citations: list[Citation] = []          # source URLs + accessed dates
    artifacts: list[ArtifactRef] = []       # draft files (listing.md, etc.)
    confidence: float                       # 0.0-1.0

# Deep role-prompt loaded from contracts/role-prompts/coordinator.md
# (Phase 00.5 first-draft deliverable; hardening pass — Phase 01.1 retro)
coordinator_agent = Agent(
    model=LLMGatewayModel(role_key="coordinator"),   # routes к deepseek-reasoner per ADR-018
    deps_type=CoordinatorDeps,
    result_type=CoordinatorOutput,
    system_prompt="<loaded from contracts/role-prompts/coordinator.md>",   # 9-section deep prompt
    tools=[delegate_task, web_search, read_url],
)
```

### Delegation tool signature

```python
# backend/src/agents/tools/delegate.py
from pydantic import BaseModel, Field
from pydantic_ai import RunContext, Tool
from uuid import UUID

class DelegateInput(BaseModel):
    target_agent_id: UUID = Field(..., description="ID of team-member agent to delegate to")
    sub_prompt: str = Field(..., description="Task description for delegate (concise, actionable)")
    context_artifacts: list[UUID] = Field(default=[], description="Optional artifact references")

class DelegateResult(BaseModel):
    sub_task_id: UUID
    output: str
    cost_credits: Decimal
    tokens_used: int
    citations: list[Citation] = []
    artifacts: list[ArtifactRef] = []

async def delegate_task(ctx: RunContext[CoordinatorDeps], inp: DelegateInput) -> DelegateResult:
    """Delegate sub-task к team-member agent.

    Per ADR-016 (team-first UX) + ADR-024 (tasks contract):
      1. Validate target_agent_id ∈ ctx.deps.available_agents
      2. Validate delegation depth: count parent_task_id chain — if >5, raise DelegationDepthExceeded
      3. Create sub-Task row в tasks.tasks (parent_task_id = ctx.deps.task_id)
      4. Insert task_step (step_type='delegation', payload={target, sub_prompt})
      5. Execute target agent via orchestrator.execute_agent_task(sub_task_id) — awaits result
      6. Audit emit (delegation.created + delegation.completed)
      7. Cost rollup: child cost added to parent в tasks.task_steps; SSE event emitted
      8. Returns DelegateResult to coordinator
    """
    ...
```

### Task state machine (per tasks contract — inline reference)

```
pending ──> running ──┬──> completed
                      ├──> failed       (LLM 503, tool error, budget exceeded)
                      └──> cancelled    (user-initiated; cascades to children)

Invariants:
  - status transitions strict (no skip; running cannot return к pending)
  - parent.cost_credits = sum(children.cost_credits) after rollup phase
  - cancellation cascades children atomically (CASCADE via tasks.parent_task_id FK)
  - max parent chain depth = 5 (DelegationDepthExceeded raised otherwise)
```

### SSE event-types для task stream

```python
# backend/src/runtime/sse_events.py
class TaskStreamEvent(BaseModel):
    """SSE payload — emitted to /api/cells/{id}/tasks/{tid}/stream subscribers.

    Per contracts/tasks/events.yaml event types — UI consumes для 00.7 frontend.
    """
    event_type: Literal[
        "task.started",
        "task.step_started",       # payload: { step_id, step_type: 'llm_call'|'tool_call'|'delegation', agent_id }
        "task.step_token",         # payload: { step_id, delta: <streaming token> }
        "task.step_completed",     # payload: { step_id, tokens, cost, duration_ms }
        "task.delegation_started", # payload: { sub_task_id, target_agent_id }
        "task.delegation_completed",
        "task.completed",          # payload: { result: CoordinatorOutput, total_cost_credits, total_duration_ms }
        "task.cancelled",
        "task.failed",             # payload: { error_type, retry_possible }
    ]
    task_id: UUID
    timestamp: datetime
    payload: dict
```

## File-tree (additions per ADR-024 §1)

```
backend/src/agents/
├── __init__.py
├── models.py                       # SQLAlchemy per contracts/agents/schema.sql
├── schemas.py                      # Pydantic per contracts/agents/api.yaml
├── routers/
│   ├── __init__.py
│   ├── teams.py                    # POST /api/cells/{id}/teams (provision from preset)
│   ├── instances.py                # GET /api/cells/{id}/agents
│   └── archetypes.py               # GET /api/agent-archetypes (read-only system catalog)
├── services/
│   ├── __init__.py
│   ├── team_provisioning_service.py    # ProvisionTeam: create AgentInstances from preset
│   └── archetype_service.py
├── coordinator.py                  # Pydantic-AI Agent (deepseek-r1, top-level orchestrator)
├── researcher.py                   # Pydantic-AI Agent (deepseek-v3, tools=[web_search, read_url])
├── writer.py                       # Pydantic-AI Agent (deepseek-v3, marketing/content artifact output)
├── analyst.py                      # Pydantic-AI Agent (deepseek-r1, LLM-only Wave 0; Pyodide W2)
├── tools/
│   ├── __init__.py
│   └── delegate.py                 # delegate_task per signature
└── events.py                       # CloudEvents per contracts/agents/events.yaml

backend/src/tasks/
├── __init__.py
├── models.py                       # SQLAlchemy per contracts/tasks/schema.sql
├── schemas.py                      # Pydantic per contracts/tasks/api.yaml
├── routers/
│   ├── __init__.py
│   ├── tasks.py                    # POST/GET /api/cells/{id}/tasks/[{tid}]
│   └── stream.py                   # GET /api/cells/{id}/tasks/{tid}/stream (SSE)
├── services/
│   ├── __init__.py
│   ├── task_service.py             # create_task, get_task, cancel_task
│   └── cost_rollup_service.py      # parent.cost = sum(children.cost) atomic update
├── events.py                       # CloudEvents per contracts/tasks/events.yaml
└── exceptions.py                   # DelegationDepthExceeded, BudgetExceeded, TaskCancelled

backend/src/runtime/
├── __init__.py
├── orchestrator.py                 # execute_agent_task — drives Agent.run + persists task_steps
├── sse_events.py                   # SSE payload models (per inline)
├── sse_publisher.py                # Redis pub/sub bridge — orchestrator → SSE subscribers
└── budget_guard.py                 # Per-task cost cap (50 T-credits default) + reservation

backend/alembic/versions/agents/
├── 0001_agent_archetypes.py
├── 0002_team_presets.py
└── 0003_agent_instances.py

backend/alembic/versions/tasks/
├── 0001_tasks.py
├── 0002_task_steps.py
└── 0003_task_artifacts.py

backend/src/agents/seed_data/
└── productivity_core_v1.py         # TeamPreset seed (preset_key='productivity_core', horizontal anchor per ADR-017)
```

## Function signatures (key services)

```python
# backend/src/runtime/orchestrator.py
async def execute_agent_task(
    task_id: UUID,
    agent: Agent,                          # Pydantic-AI agent instance
    user_prompt: str,
    deps: BaseModel,
    sse_publisher: SSEPublisher,
    db: AsyncSession,
) -> CoordinatorOutput:
    """Drive Agent.run() with task-step persistence + SSE emission.

    Flow:
      1. Reserve budget (BudgetGuard check; raise BudgetExceeded if <50 T-credits)
      2. Emit SSE 'task.started'
      3. Persist task_step (type='llm_call', started_at) before each LLM call
      4. Stream agent.run_stream() — emit SSE 'task.step_token' per delta
      5. On tool_call (web_search, read_url, delegate_task) — persist task_step type='tool_call'/'delegation'
      6. On agent completion — persist final task_step (type='llm_call', tokens, cost, duration)
      7. Cost rollup: aggregate task_steps → task.cost_credits
      8. Emit SSE 'task.completed' with final CoordinatorOutput JSON
      9. Audit emit
      10. Refund unused budget (reserved - actual)
    """
    ...

# backend/src/tasks/services/task_service.py
async def create_task(
    cell_id: UUID,
    user_id: UUID,
    prompt: str,
    parent_task_id: UUID | None = None,
) -> Task:
    """Per contracts/tasks/api.yaml POST /api/cells/{id}/tasks.

    Flow:
      1. Validate cell.status='active', user has membership
      2. Validate parent depth (if parent_task_id) — raise DelegationDepthExceeded if >5
      3. Insert tasks.tasks row (status='pending')
      4. Lookup coordinator agent_instance в cell
      5. Schedule orchestrator.execute_agent_task() background — non-blocking
      6. Return task_id (status='pending' → will transition к 'running' immediately)
    """
    ...

# backend/src/tasks/services/cost_rollup_service.py
async def rollup_task_cost(task_id: UUID, session: AsyncSession) -> Decimal:
    """Aggregate task_steps.cost → task.cost_credits. Recursive для parent chain.

    Atomic: single transaction. Walks parent_task_id chain up к root, recalculates
    each ancestor's cost_credits from sum(direct task_steps + children.cost).
    """
    ...
```

## Tests (per P-INIT-1: ≥1 unit + ≥1 integration в виде кода)

### Unit test — delegation routing

```python
# backend/tests/agents/test_delegate_tool.py
import pytest
from uuid import uuid4
from unittest.mock import AsyncMock
from src.agents.tools.delegate import delegate_task, DelegateInput
from src.agents.exceptions import DelegationDepthExceeded

@pytest.mark.asyncio
async def test_delegation_depth_limit_enforced():
    """Unit: delegation chain >5 raises DelegationDepthExceeded."""
    # Arrange: simulate chain of 5 ancestors via task.parent_task_id
    ctx = AsyncMock()
    ctx.deps.task_id = uuid4()
    ctx.deps.max_delegation_depth = 5
    task_service = AsyncMock()
    task_service.get_parent_chain_depth.return_value = 5

    inp = DelegateInput(
        target_agent_id=uuid4(),
        sub_prompt="Test sub-task",
    )

    # Act + Assert
    with pytest.raises(DelegationDepthExceeded):
        await delegate_task(ctx, inp, task_service=task_service)

    task_service.get_parent_chain_depth.assert_awaited_once()
```

### Integration test — full Coordinator→Researcher+Analyst+Writer flow via TestModel

```python
# backend/tests/agents/test_coordinator_full_flow.py
import pytest
from pydantic_ai.models.test import TestModel
from src.agents.coordinator import coordinator_agent, CoordinatorDeps

@pytest.mark.integration
@pytest.mark.asyncio
async def test_coordinator_delegates_to_three_specialists(
    db_session, sample_cell, sample_user, mock_sse_publisher
):
    """Integration: end-to-end productivity-core demo flow («Market & content brief»).

    Coordinator receives: 'Запускаем платформу AI-команд для SMB в РФ. Сделай нам
                          market brief + контент-план первого месяца'
      → decomposes на 3 sub-task
      → delegates параллельно: Researcher (web_search для конкурентов / boards / trends),
        Analyst (TAM/SAM + competitive matrix + positioning),
        Writer (brief.md + content-plan.md + tone-of-voice)
      → synthesizes 3 артефакта + CoordinatorOutput
      → SSE events emitted in expected order
      → cost rolled up: parent.cost == sum(children.cost)
      → result persisted в tasks.tasks
    """
    # Arrange: use Pydantic-AI TestModel (deterministic) instead of real LLM
    coordinator_agent.override(model=TestModel(call_tools=["delegate_task"]))

    deps = CoordinatorDeps(
        cell_id=sample_cell.id,
        task_id=uuid4(),
        user_id=sample_user.id,
        available_agents=[researcher_agent_instance, analyst_agent_instance, writer_agent_instance],
    )

    # Act
    result = await coordinator_agent.run(
        user_prompt="Запускаем платформу AI-команд для SMB в РФ. Сделай нам market brief + контент-план первого месяца",
        deps=deps,
    )

    # Assert structured output
    assert isinstance(result.data, CoordinatorOutput)
    assert "brief" in result.data.summary.lower() or "план" in result.data.summary.lower()
    assert len(result.data.delegation_plan) >= 3     # Researcher + Analyst + Writer
    assert len(result.data.citations) >= 3            # ≥3 verifiable sources from Researcher
    assert len(result.data.artifacts) >= 3            # brief.md + competitive-matrix.md + content-plan.md

    # Assert SSE events emitted в expected order
    emitted_events = [call.args[0].event_type для call in mock_sse_publisher.publish.call_args_list]
    assert "task.started" in emitted_events
    assert "task.delegation_started" in emitted_events
    assert "task.delegation_completed" in emitted_events
    assert "task.completed" in emitted_events

    # Assert cost rollup
    parent_task = await db_session.get(Task, deps.task_id)
    children = await db_session.execute(select(Task).where(Task.parent_task_id == deps.task_id))
    expected_cost = sum(c.cost_credits for c in children.scalars())
    assert parent_task.cost_credits == expected_cost
```

## Tasks

1. **Migrations** — `agents/0001-0003` per `contracts/agents/schema.sql`; `tasks/0001-0003` per `contracts/tasks/schema.sql`
2. **TeamPreset seed-data** — `agents/seed_data/productivity_core_v1.py` (preset_key='productivity_core', agent_bundle=[coordinator, researcher, writer, analyst], workflow_dag = parallel-3, vertical_tag=null, horizontal=true)
3. **Pydantic-AI integration** — `LLMGatewayModel` wrapper в `backend/src/llm_gateway/pydantic_ai_model.py` (Phase 00.4 deliverable extension)
4. **Role-prompt loader** — `services/role_prompt_loader.py`: parse `contracts/role-prompts/<role>.md` (YAML frontmatter + 9 sections) → composed system-prompt string
5. **Coordinator agent** — `coordinator.py` per inline (system_prompt loaded from `contracts/role-prompts/coordinator.md`; deepseek-r1)
6. **Researcher agent** — `researcher.py` (deepseek-v3, tools=[web_search, read_url], rate-limited через 00.4 built-in; system_prompt из `contracts/role-prompts/researcher.md`)
7. **Writer agent** — `writer.py` (deepseek-v3, marketing/content artifact output; system_prompt из `contracts/role-prompts/writer.md`)
8. **Analyst agent** — `analyst.py` (deepseek-r1, LLM-only reasoning Wave 0, Pyodide capability gap documented; system_prompt из `contracts/role-prompts/analyst.md`)
9. **delegate_task tool** — `agents/tools/delegate.py` per signature; depth check + sub-task creation + cost rollup
10. **Task service** — `task_service.create_task()` + `get_task()` + `cancel_task()` (cascading cancel children)
11. **Orchestrator** — `runtime/orchestrator.execute_agent_task()` per signature; SSE publishing + task_step persistence + parallel sub-task spawn для Researcher+Analyst+Writer
12. **SSE publisher** — `runtime/sse_publisher.py` (Redis pub/sub bridge between orchestrator + `/stream` endpoint)
13. **Budget guard** — `runtime/budget_guard.py` (50 T-credit reservation per task; refund unused; raise BudgetExceeded если sum_charges > cap)
14. **Cost rollup** — `tasks/services/cost_rollup_service.rollup_task_cost()` per signature, atomic walk up parent chain
15. **API routers** — `tasks/routers/tasks.py` (CRUD) + `tasks/routers/stream.py` (SSE GET) + `agents/routers/teams.py` (POST team provisioning)
16. **CloudEvents emitters** — `tasks/events.py` + `agents/events.py` per respective events.yaml
17. **Memory integration** — Wave 0 reads/writes `cell_<uuid>.memory_entries` directly через pgvector (Phase 00.3 setup); полная memory contract Wave 1
18. **Demo scenario script** — `backend/scripts/demo_market_brief.py` runs «Market & content brief» end-to-end against staging deploy; asserts артефакт contracts (brief ≥1500w, matrix ≥5×4, plan = 10 posts)
19. **Tests** — unit (delegation depth + cost rollup + role-prompt loader) + integration (Coordinator→[Researcher+Analyst+Writer] parallel per inline TestModel test) + cancel cascade + SSE event order + coverage ≥75%

## Acceptance Criteria (testable checks)

- [ ] **AC1.** User регистрируется (00.2) → cell со spawn'ом `productivity-core` horizontal team создаётся автоматически. Verified via `test_register_provisions_productivity_core_team`.
- [ ] **AC2.** `POST /api/cells/{id}/tasks {prompt: "..."}` → 202 + `{task_id, status: 'pending'}` per `tasks/api.yaml`. Coordinator picks up в <2s.
- [ ] **AC3.** `GET /api/cells/{id}/tasks/{tid}` → status transitions `pending` → `running` → `completed` per task state machine.
- [ ] **AC4.** Result contains structured `CoordinatorOutput` (summary + delegation_plan ≥3 entries + citations ≥3 + artifacts ≥3 + confidence). Verified via integration test.
- [ ] **AC5.** SSE стримит прогресс через `/api/cells/{id}/tasks/{tid}/stream` в expected event order (task.started → 3 parallel delegation events → step events → task.completed). SSE latency < 200ms p95.
- [ ] **AC6.** Cost rollup accuracy: `parent.cost_credits == SUM(children.cost_credits + direct_steps.cost)`. Verified via `test_cost_rollup_atomic`.
- [ ] **AC7.** Internal demo: team members могут зайти на staging и запустить «Market & content brief» сценарий через UI (Phase 00.7 dependency). Wave 0 demo runs against staging deployment.
- [ ] **AC8.** Demo end-to-end latency p95 ≤ **120s** (3 parallel sub-task delegations). Verified via `demo_market_brief.py` script + load test (10 runs).
- [ ] **AC9.** Demo артефакты соответствуют contracts: `brief.md` ≥1500 words, `competitive-matrix.md` ≥5 rows × ≥4 columns, `content-plan.md` ровно 10 posts с outline.
- [ ] **AC10.** Cost: demo run ≤30¢ per execution (LLM-cost rollup verified via `task.cost_credits` field).
- [ ] **AC11.** Role-prompts loaded: `coordinator.md`, `researcher.md`, `writer.md`, `analyst.md` присутствуют в `contracts/role-prompts/`, валидны (frontmatter parses + 9 sections present), и используются Pydantic-AI agents.
- [ ] **AC12.** Cancel cascade: `POST /api/cells/{id}/tasks/{tid}/cancel` → parent.status='cancelled' + ALL descendant tasks.status='cancelled' атомарно. Verified via `test_cancel_cascades_to_children`.
- [ ] **AC13.** Coverage ≥75% для `backend/src/agents/` + `backend/src/tasks/` + `backend/src/runtime/`.
- [ ] **AC14.** Role-prompt hardening backlog зафиксирован — список replicate-failures из demo runs передаётся в Phase 01.1 retro как input для hardening pass.

## Risks

- **R-02** (template качество): `productivity-core` deep role-prompts — first-draft в Phase 00.5, hardening pass scheduled в Phase 01.1 retro per AC14. Quality bar — «работает на demo-сценарии, итерации после reality-check»
- **R-04** (multi-agent cost): max-depth=5 + per-task cost cap 50 T-credits + budget guard prevents runaway (AC6 + AC10)
- **R-31** (AI-cost overrun): Phase 00.5 testing runs DeepSeek primary (cheapest per ADR-018); production runs scope-limited через `dev_team` budget partition (P-AUDIT-4)
- **R-NEW (LLM-only Analyst):** Analyst Wave 0 без Pyodide — высокий риск hallucination на числовых оценках. Mitigated через role-prompt requirements: явные допущения, range estimates вместо точечных, verifiable sources, capability-gap callouts для Pyodide

## ADR refs

- [ADR-003](../../../decisions/ADR-003-pydantic-ai-runtime.md) — Pydantic-AI choice
- [ADR-016](../../../decisions/ADR-016-team-first-ux.md) — team-first UX, no free-form agent comms
- [ADR-017](../../../decisions/ADR-017-vertical-templates.md) (revision 2026-05-15) — horizontal `productivity-core` anchor + 5 vertical-templates roadmap
- [ADR-022](../../../decisions/ADR-022-coordinator-wizard-llm-hybrid.md) (revision 2026-05-15) — Coordinator top-level в horizontal preset; bifurcation horizontal vs vertical
- [ADR-024](../../../decisions/ADR-024-bounded-context-contracts.md) — `agent_archetype_id` (FK к `agents.agent_archetypes`); `ui_sprite_archetype` deprecated
- [ADR-029](../../../decisions/ADR-029-master-agent-vertical-templates.md) — Master-Agent layer (NOT applied в Wave 0 horizontal preset; W1+ для verticals)

## Notes for AI-agents

- **НЕ** implement свободную межагентную коммуникацию. Только Coordinator-делегирование через `delegate_task` tool (per ADR-016). В Wave 0 horizontal preset — Coordinator top-level (без Master-Agent); Master-Agent layer добавится в Wave 1 phase 01.1 для vertical-templates per [ADR-029](../../../decisions/ADR-029-master-agent-vertical-templates.md).
- Workflow templates (visual editor) — Wave 3+
- Memory (cell + role) деталь Wave 1; Wave 0 пишет в `cell_<uuid>.memory_entries` напрямую (pgvector HNSW из Phase 00.3)
- Deep role-prompts (4 файла в `contracts/role-prompts/`) — first-draft в этой фазе. Hardening pass scheduled в Phase 01.1 retro per AC14 — Phase 01.1 retro принимает на input список replicate-failures из demo runs и backlog «непонятного поведения» из internal demo
- Wave 0 не имеет vertical golden-dataset (verticals существуют только W1+). Quality bar — demo scenario passes acceptance criteria
- Agent archetype IDs привязаны к canonical names в `agents.agent_archetypes`; реальные sprite assets — Wave 2 (ADR-021)
- Pydantic-AI TestModel используется в integration tests для deterministic flow validation (no real LLM cost)
- При encounter contract gap — escalate к architect, не правь contracts сам (per P-INIT-2)
- **LLM-only Analyst:** Analyst в Wave 0 работает без Pyodide. Role-prompt (см. `contracts/role-prompts/analyst.md`) явно требует assumption-lists, range estimates, source citations, capability-gap callouts. Pyodide capability добавляется в Wave 2.

## ui-spec (minimal — task-progress SSE payload contract for Phase 00.7 consumer)

```yaml
ui-spec:
  consumers:
    - phase: 00.7
      surface: cells/{cellId}/tasks/{taskId}/result
      consumes_sse: tech.oriion.tasks.stream.v1
      events_handled:
        - task.started
        - task.step_started
        - task.step_token       # streaming token chunks (deepseek-reasoner reasoning visible)
        - task.step_completed
        - task.delegation_started
        - task.delegation_completed
        - task.completed
        - task.cancelled
        - task.failed
      payload_invariants:
        - timestamp ISO 8601 + UTC
        - cost_credits Decimal serialized as string (avoid float precision loss)
        - step_id stable per task_step row
      a11y-must-have:
        - aria-live=polite for streaming token updates
        - аnnounce final result via role=status
```

# SKELETON — Wave 1 deliverable (per ADR-024)
# Detailed event payload schemas deferred to Milestone D, Wave 1 phase
# Status: structural placeholder for cross-context references

specversion: "1.0"
context: memory
status: skeleton

events:
  - type: oriion.memory.entry.upserted.v1
    description: "Memory entry created or updated (cell_memory or role_memory)"
    source: oriion.memory
    dataschema: "TODO: link to JSON Schema in Milestone D"
    data: {}

  - type: oriion.memory.entry.expired.v1
    description: "Memory entry exceeded ttl_seconds and was pruned by the expiry sweeper"
    source: oriion.memory
    dataschema: "TODO"
    data: {}

  - type: oriion.memory.index.rebuilt.v1
    description: "HNSW vector index rebuilt for a memory scope (cell or archetype)"
    source: oriion.memory
    dataschema: "TODO"
    data: {}

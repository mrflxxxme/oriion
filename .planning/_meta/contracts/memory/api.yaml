# SKELETON — Wave 1 deliverable (per ADR-024)
# Detailed OpenAPI spec deferred to Milestone D, Wave 1 phase
# Status: structural placeholder for cross-context references
#
# NOTE: Actual semantic vector search at runtime is served by AgentDB MCP
# (HNSW index, ONNX all-MiniLM-L6-v2 384-dim, per ADR-023 §6-7).
# This HTTP API is the CONTROL PLANE only — registration, upsert orchestration,
# search proxy / fallback. Hot-path search clients should call AgentDB MCP directly.

openapi: 3.1.0
info:
  title: Oriion Memory API (SKELETON)
  version: 0.0.1-skeleton
  description: |
    Memory context — cell-level + agent-archetype-level long-term memory.
    Control-plane API; semantic search executes via AgentDB MCP.
    SKELETON — definitions deferred to Milestone D, Wave 1.

paths:
  /memory/cells/{cell_id}/search:
    post:
      summary: "TODO: define в Milestone D — semantic search within a cell's memory"
      operationId: searchCellMemory
      parameters:
        - name: cell_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SemanticSearchRequest'
      responses:
        '200':
          description: TODO
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SemanticSearchResult'

  /memory/cells/{cell_id}/upsert:
    post:
      summary: "TODO: define в Milestone D — upsert a cell memory entry"
      operationId: upsertCellMemory
      parameters:
        - name: cell_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryEntry'
      responses:
        '200':
          description: TODO
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryEntry'

  /memory/roles/{archetype_id}/search:
    post:
      summary: "TODO: define в Milestone D — semantic search within an agent archetype's memory"
      operationId: searchRoleMemory
      parameters:
        - name: archetype_id
          in: path
          required: true
          description: agent_archetype_id (per P-AUDIT-2 naming)
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SemanticSearchRequest'
      responses:
        '200':
          description: TODO
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SemanticSearchResult'

  /memory/roles/{archetype_id}/upsert:
    post:
      summary: "TODO: define в Milestone D — upsert a role memory entry"
      operationId: upsertRoleMemory
      parameters:
        - name: archetype_id
          in: path
          required: true
          description: agent_archetype_id (per P-AUDIT-2 naming)
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryEntry'
      responses:
        '200':
          description: TODO
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryEntry'

components:
  schemas:
    MemoryEntry:
      type: object
      description: "TODO: define в Milestone D (namespace, key, value, optional ttl)"
      properties: {}

    SemanticSearchRequest:
      type: object
      description: "TODO: define в Milestone D (query text or embedding, k, filters)"
      properties: {}

    SemanticSearchResult:
      type: object
      description: "TODO: define в Milestone D (entry + distance + score)"
      properties: {}
